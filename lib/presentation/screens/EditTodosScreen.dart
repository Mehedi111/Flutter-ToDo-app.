
import 'package:flutter/material.dart';

class EditTodosScreen extends StatelessWidget {
  const EditTodosScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "Edit todos screen"
        ),
      ),
    );
  }
}
